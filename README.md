Como rodar o código:

Abra o Prompt de Comando e crie uma pasta, onde colocará todos os arquivos baixados pelo GitHub. Abra a pasta no Desktop e inicie o código, depois digite o comando "node server.js" para o servidor começar a rodar. Abra o navegador e pesquise por localhost:3000.
